# JMeter User Properties for Load Test Configuration
# These properties can be overridden via -D flags on command line

# Config server URL
config.server.url=http://localhost:8080

# Number of concurrent threads (virtual users)
test.threads=5

# Number of iterations per thread
test.iterations=3
